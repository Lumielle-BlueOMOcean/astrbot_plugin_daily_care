# Changelog

## v1.1.14

本版本集中改进主动关怀稳定性、数据升级保护和运行兼容性。

### 修复

- 修复主动关怀消息发送后，后续普通聊天可能长时间无响应的问题。
- 修复主动关怀任务长时间未完成时，新的关怀任务可能重复积压的问题；过期消息不会在恢复后突然发送。
- 修复发送失败、框架异常和主动选择不打扰时的状态收尾，避免失效消息被发送或写入聊天记录。
- 修复插件升级后运行数据仍写入插件代码目录的问题，避免更新插件时误影响已有数据。
- 修复缺少异步网络依赖时 IP 定位可能阻塞 AstrBot 异步事件循环的问题；依赖不可用时会安全返回定位失败。

### 改进

- 改进主动消息的发送记录与聊天历史同步，减少重复记录及发送状态不一致的问题。
- 使用 AstrBot 标准插件数据目录保存 SQLite 数据。首次从旧版插件目录迁移时，必须先停止 AstrBot Core、运行升级前数据保护工具，并在 Core 保持停止期间完成插件文件替换；工具以 SQLite 一致性备份方式保护旧数据库并保留旧文件作为恢复副本。具体步骤见 README 的升级指南。
- 完善旧版数据库、WAL 数据和迁移失败时的数据保护；改进保护记录校验，避免 SQLite 辅助文件的正常变化被误判，并在保护记录写入失败时保留新旧数据库，提供可验证的恢复路径。
- 完善主动唤醒生命周期、数据迁移和升级保护测试，已在 AstrBot 4.25.5 和 4.28.0 环境下通过基础运行验证。

### 兼容性

- 继续支持 AstrBot `>=4.24.0`。

## v1.1.12 · 2026-09-12

本版本集中完善主动唤醒的安全性和可靠性。

### 修复

- 修复主动唤醒时的内部判断、执行说明、协议内容或错误信息意外显示给用户的问题。
- 主动唤醒改为严格的“发送”或“静默”结果；无法可靠识别的结果会自动静默，不会把原始内容直接发出。
- 收紧主动消息的发送边界，经过修改、重复发送、附带额外内容的消息都会被拦截。
- 只有平台确认消息发送成功后，才更新聊天历史、发送次数、冷却时间和计划状态。
- 修复主动唤醒失败后计划可能一直停留在处理中状态的问题。

### 改进

- 主动消息始终由当前 AstrBot Main Agent 结合真实会话、人格、记忆和正常插件链路生成，Daily Care 不会代替 Main Agent 编写或发送用户话术。
- 主动唤醒默认不使用工具，也不采用流式输出，同时保留其他正常 Main Agent 插件的参与。
- 完善 AstrBot 4.25.5 的基础运行兼容性验证和自动检查。

### 兼容性

- 插件最低支持 AstrBot `>=4.24.0`。

## v1.1.8

### 修复

- 修复用户与 bot 正在聊天时仍可能突然出现冷场关怀的问题；bot 的正常回复也会计入对话活跃状态。

### 改进

- 主动唤醒只输出面向用户的自然话语，不再带出内部状态、执行记录、系统提示或旁白。
- 决策时会参考最近双方的交流情况；对话活跃时，除极端天气或明显不适外会尽量不打扰。

## v1.1.7

### 改进

- 改进平台实例和会话地址的自动识别，适配不同的 AstrBot 平台实例名称。
- 自动识别失败时会自动重试，并在多次失败后提示在 WebUI 中选择平台实例。
- WebUI 的关怀对象设置增加平台实例选择和连接状态提示。

### 修复

- 修复计划关怀时间显示不准确的问题，执行计划时会以当前时间为准。

## v1.1.6 · 2026-08-19

### 新增

- 关怀对象支持卡片列表，每个对象可以单独设置 QQ、称呼、关系和城市；天气与状态关怀会分别评估各个对象。
- 首次收到消息时可以自动识别默认对象的 QQ，也可以在 WebUI 中手动填写 QQ 数字，无需填写 umo。
- WebUI 支持省、市、区县三级城市选择。

### 改进

- 自动识别平台实例和城市信息，手动设置的城市优先使用。
- 城市变化时自动清理旧天气缓存，并清理失效的定位记录。

### 修复

- 修复关怀对象设置中省份下拉框无法选择的问题。

## v1.1.5

### 新增

- 新增晚安识别和动态休息窗口：用户说晚安或准备睡觉后，插件会暂时停止主动消息；用户再次发消息即可恢复。
- 晚安识别同时支持关键词和 LLM 判断，并统一管理勿扰状态。
- 新增休息时长、最近话题和主动关怀互斥等配置。

### 改进

- 冷场主动消息会按场景选择是否接续最近话题，天气和状态关怀不会注入无关话题。
- 主动关怀与状态/计划关怀增加相似内容的间隔控制，天气提醒不受该限制。

### 修复

- 修复主动消息反复引用 bot 自己内容的问题，话题提取只读取用户真实消息。

## v1.1.4 · 2026-08-17

### 改进

- 扩充插件市场关键词，增加陪伴、主动聊天、长期记忆、情绪和状态关怀等搜索词。

## v1.1.3 · 2026-08-17

### 改进

- 重新整理 WebUI，将“理解与决策”拆分为“决策引擎”和“状态与计划关怀”，并将天气工具开关归入天气设置。

### 修复

- 修复 WebUI 加载失败的问题，并让错误提示显示具体原因。

## v1.1.2 · 2026-08-17

### 修复

- 修复天气相关配置保存后恢复默认的问题。

## v1.1.1 · 2026-08-17

### 改进

- 将静默时间改为根据双方交流重新计算，避免主动消息按照固定时间连续发送。
- 删除“两次主动最小间隔”配置，由静默时间统一控制主动消息间隔。

## v1.1.0 · 2026-08-16

### 新增

- 常态天气提示支持多选时段、自定义 `HH:MM-HH:MM` 时段（含跨天）和两条提示之间的最小间隔。

### 修复

- 修复间隔配置为 `0` 时被错误当作默认间隔的问题。

## v1.0.0 · 2026-08-16

### 首次发布

- 发布微光-Daily Care 首版，提供天气感知、状态反思、主动关怀、勿扰时段和 WebUI 配置能力。
