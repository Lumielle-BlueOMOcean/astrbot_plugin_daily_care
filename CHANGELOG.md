# Changelog

> 发布状态说明：v1.0.0 至 v1.1.8 的版本记录来自仓库既有发布说明与 README；其中 v1.1.3、v1.1.6 有 GitHub Release，v1.1.4 有 Git tag。v1.1.9 至 v1.1.11 仅存在开发提交，已并入 v1.1.12，不作为独立正式 Release。v1.1.12 已有 Git tag 但当前远端未发现 GitHub Release；v1.1.13 是当前发布准备版本，尚未创建 Tag 或 GitHub Release。无法由正式发布记录确认的日期不补写。

## v1.1.13（当前发布准备版本；未创建 Tag/Release）

相对于 v1.1.12，本版本聚焦主动唤醒生命周期和会话恢复。

### 修复

- 修复主动关怀消息发送后的收尾路径重复获取同一会话锁，导致后续普通聊天可能长时间等待的问题。
- 修复旧唤醒尚未完成时内部 claim 超时释放后仍反复入队的问题；过期唤醒会保持失效，不会在恢复后意外发送。
- 修复平台发送失败、框架异常和主动沉默路径的终态清理，避免唤醒或计划状态悬挂、重复发送或错误写入历史。

### 改进

- 收紧平台实际发送确认、真实历史写回和发送状态更新的顺序，并增加重复处理保护。
- 增加 AstrBot 4.28.0 运行时兼容性 smoke 和主动唤醒生命周期回归测试，同时保留 AstrBot 4.25.5 smoke。

## v1.1.12（2026-09-12；已创建 tag v1.1.12，未发现 GitHub Release）

相对于上一正式版本 v1.1.8 的稳定版本更新。v1.1.9、v1.1.10 和 v1.1.11 是开发阶段中间版本，本版本统一收录其主动唤醒安全修复。

### 修复

- 修复主动唤醒把 Main Agent 的内部判断、执行总结、协议内容或框架错误泄漏给用户的问题。
- 主动唤醒改为严格的 SEND/SILENT 协议，无法可靠解析的模型输出统一静默处理。
- 收紧主动唤醒的最终传输边界：只有经过验证的正文允许发送，重复、改写、附加组件和错误文本均被拦截。
- 修复唤醒入队与实际平台发送混淆的问题；只有平台确认发送成功后才更新历史、计数、冷却和计划状态。
- 修复主动唤醒失败后计划可能停留在 processing 的生命周期问题。

### 改进

- 主动消息始终由当前 AstrBot Main Agent 结合真实会话、人格、记忆和正常插件链路生成，Daily Care 不再独立生成或直接代替 Main Agent 发言。
- 主动唤醒默认禁用工具和流式输出，同时保留 Lumielle Echo 等正常 Main Agent hook 的参与。
- 增加真实 AstrBot 4.25.5 runtime smoke 与 GitHub Actions CI，覆盖插件加载、事件继承、消息组件兼容性和主动消息传输边界。

### 兼容性

- 插件最低支持 AstrBot `>=4.24.0`。

### v1.1.9–v1.1.11（开发阶段中间版本，未作为独立正式 Release）

- 这些版本对应主动唤醒生命周期、实际发送确认和传输边界的连续开发提交，最终作为 v1.1.12 的整体安全修复发布；不在正式版本历史中重复拆列。

## v1.1.8（历史正式版本记录；未发现可确认的独立 Tag/Release 日期）

### 修复

- 修复对话进行中仍可能突然触发冷场主动关怀的问题：bot 的正常被动回复也会刷新活跃基准，避免热聊时插入主动消息。

### 改进

- 主动唤醒增加输出边界约束，只允许输出面向用户的自然话语，禁止带出内部状态、执行记录、系统提示、工作日志或旁白。
- 决策引擎增加“最近双方交流”上下文；对话活跃期内除极端天气或明显不适外倾向静默。

## v1.1.7（历史正式版本记录；未发现独立 Tag/Release）

### 改进

- 重构 UMO 与平台实例解析，移除 `Lumielle` 硬编码兜底；解析失败时停止本轮唤醒，避免生成幽灵会话。
- 增加平台实例解析的 60 秒 TTL 缓存、失败重试和连续失败日志引导。
- WebUI 的关怀对象区域增加平台实例下拉选择和 UMO 诊断信息，并新增 `/page/platforms` 诊断接口。

### 修复

- 修复计划关怀时间错位：执行计划时以当前真实时间为准，避免使用创建计划时已经过期的时间背景。

## v1.1.6（2026-08-19；GitHub Release 与 tag v1.1.6）

### 新增

- 关怀对象从单个默认目标升级为对象卡片列表，每个对象独立配置 QQ、称呼、关系和城市；天气与状态关怀按对象评估和路由。
- 首次收到用户消息时可自动捕获默认对象 QQ；自动捕获不稳定时可在 WebUI 手动填写 QQ 数字，无需填写 umo。
- WebUI 关怀对象管理支持省、市、区县三级级联城市选择。

### 改进

- 平台 ID 改为动态解析，适配不同平台实例名称。
- 动态定位记录唯一化；城市变化时清理旧天气缓存，启动时自愈孤儿定位记录；手动指定城市优先于 IP 定位。

### 修复

- 修复关怀对象弹窗省份下拉无法选择的问题。

## v1.1.5（历史正式版本记录；未发现独立 Tag/Release）

### 新增

- 新增晚安识别与动态休息窗口：用户说晚安或睡了后暂时不触发主动消息，用户再次发消息会打破窗口。
- 晚安识别同时支持关键词和反思 LLM 兜底，并统一由 `core/rest.py` 管理勿扰判定。
- 新增 `rest_after_sleep_hours`、`enable_recent_topic` 和 `silence_exclude_window_min` 配置。

### 改进

- 冷场主动按场景接续最近话题，天气和状态关怀不再注入无关话题摘要。
- 主动关怀与状态/计划关怀增加沉默类互斥，天气提醒不参与该互斥。

### 修复

- 修复主动消息自我强化：话题提取只读取用户真实消息，不再摘取 bot 自己的主动消息。

## v1.1.4（2026-08-17；已创建 tag v1.1.4，未发现 GitHub Release）

### 改进

- 扩充插件市场关键词，增加陪伴、主动聊天、长期记忆、情绪和状态关怀等搜索词，改善插件检索曝光。

## v1.1.3（2026-08-17；GitHub Release 与 tag v1.1.3）

### 改进

- 重整 WebUI 板块，将“理解与决策”拆分为“决策引擎”和“状态与计划关怀”，并将天气工具开关归入天气板块。

### 修复

- 修复 WebUI 加载失败的表单元数据引用错误，并将失败提示改为红色且显示具体原因。

## v1.1.2（2026-08-17；作为 v1.1.3 Release 的此前版本记录）

### 修复

- 修复天气相关配置保存后恢复默认的问题，补齐 WebUI 保存白名单。

## v1.1.1（2026-08-17；作为 v1.1.3 Release 的此前版本记录）

### 改进

- 将静默基准改为双方任一方的交流；主动消息成功后会重新开始累计静默时间，避免按固定计时连续发送。
- 删除“两次主动最小间隔”配置，由静默语义统一承担间隔控制。

## v1.1.0（2026-08-16；作为 v1.1.3 Release 的此前版本记录）

### 新增

- 常态天气提示支持多选倾向时段、自定义 `HH:MM-HH:MM` 时段（含跨天）和两条提示之间的最小间隔。

### 修复

- 修复间隔配置为 `0` 时被错误当作默认间隔的问题。

## v1.0.0（2026-08-16；作为 v1.1.3 Release 的此前版本记录）

### 首次发布

- 发布微光-Daily Care 首版，提供天气感知、状态反思、主动关怀、勿扰时段和 WebUI 配置能力。
